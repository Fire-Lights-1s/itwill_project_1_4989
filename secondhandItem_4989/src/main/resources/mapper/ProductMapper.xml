<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mapper.ProductMapper">
    <insert id="insertProduct">
        insert into Product (category_name, product_name, product_price, year_purchase, product_desc, product_img1, trade_method, pay_method, trade_area)
        values (#{category_name}, #{product_name}, #{product_price}, #{year_purchase}, #{product_desc}, #{product_img1}, #{trade_method}, #{pay_method}, #{trade_area})
    </insert>

	
</mapper>